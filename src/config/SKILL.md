# Config Skill Notes

Use this folder for stable shared configuration.

## Rules

- Put library-wide design tokens here
- Prefer updating existing tokens over introducing one-off literals when a pattern is clearly shared
- Keep this folder framework-neutral and dependency-light
