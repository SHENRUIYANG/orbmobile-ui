# Pad Hooks

This folder contains hooks for native pad-style layout behavior.

## Current API

- `usePadLayout`: exposes orientation, viewport size, and a side-panel visibility hint

## Intent

- Keep adaptive touch-layout helpers here
- Keep the hook generic enough to reuse across tablet-oriented screens
