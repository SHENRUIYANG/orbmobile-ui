# Pad Hooks Skill Notes

Use this folder for touch-layout hooks used by native pad flows.

## Rules

- Keep hooks presentation-focused
- Return layout hints instead of screen-specific behavior
- Export from `Pad/Hooks/index.ts` and `Pad/index.ts`
