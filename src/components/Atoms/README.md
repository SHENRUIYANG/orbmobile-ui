# Atoms

基础原子组件目录（最小可复用 UI 单元）。

## 使用方式

- 从包入口直接引入：`import { ... } from 'orbcafe-ui'`
- 原子组件应保持无业务状态，作为 Molecules/StdReport 等上层组件的拼装基础。

## 说明

- 该目录下组件通常只负责单一职责（输入、上传、文本等）。
- 业务组合请放在 `Molecules` 或更高层结构中。
