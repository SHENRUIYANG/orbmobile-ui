# Molecules

中层组合组件目录（由多个 Atoms 组合形成）。

## 使用方式

- 直接从包入口引入：`import { ... } from 'orbcafe-ui'`
- 若需要二次封装，建议保持 props 透传，避免破坏主题和样式一致性。

## 典型组件

- `CDateRangePicker`
- `CLayoutManagement`
- 其他表单/弹窗/组合交互组件
