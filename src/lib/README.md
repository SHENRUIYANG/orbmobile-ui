# Lib

`lib/` contains small shared utilities used across the package.

## Public API

- `mergeStyles`: merges style-like objects while filtering falsy values

## Intent

- Keep utilities tiny, generic, and dependency-free
- Avoid turning this folder into a second application layer
